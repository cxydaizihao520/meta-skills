# 经验日志

按时间倒序追加。通用规律请同时归位到对应场景文件；这里保留具体案例与项目特定经验（打项目标签）。

格式建议：`YYYY-MM-DD [项目] 现象 → 为什么重要 → 下次的规则`

---

- `[onn-chatbi]` 查询工具不要把「权限拒绝」当成「未找到」→ 否则会误导用户且可能让 agent 反复重试 → 保留「存在但受限」，见 `permission-and-security.md`。
- `[onn-chatbi]` 权限功能只做了生成前指导、漏了生成后强制 → 会被绕过 → 塑形与强制必须并存。
- `[onn-chatbi]` 本体工作台 UX 调整只改了当前弹窗 → 同级对话框/相邻 tab 出现不一致 → 改动时检查相邻组件，见 `frontend-ux.md`。
- `[onn-chatbi]` 前端视觉改动只验证了 expanded 状态 → compact 下错位 → 多状态一并验证。
