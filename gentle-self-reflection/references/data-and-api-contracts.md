# 数据与接口契约

- **返回结构稳定**：不要随意改返回形状；ReAct / agent 循环可能依赖固定结构，破坏它会导致误解或死循环。
- **客户端历史不可信**：客户端保存的历史不是可信的提示源（prompt source）。只持久化用于回放的安全展示块，绝不持久化敏感负载（SQL/工具负载、数据源标识、物理字段行、内部分段等）。
- **写入保持完整性**：写操作要保持后端完整性；前端默认值或控件不得替代「加载失败」的当前状态。
- **保留语义**：跨层传递时保留「存在但受限」等关键语义，不要在序列化/降级中丢失。
