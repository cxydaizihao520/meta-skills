# 前端 / UX

- **多状态一致性**：视觉/行为改动要同时检查 compact 与 expanded、admin 与 user 等不同状态，别只改一个。
- **相邻组件一致**：调整某个组件时，检查同级对话框、相邻 tab、并行页面是否需要一起改，避免单边修复。
- **视觉基线**：以设计导出/原型为视觉基线，逐页推进，文案用业务语言、与原型一致。
- **措辞扫描**：收尾时扫一遍措辞是否一致、状态文案是否对应正确语义。
